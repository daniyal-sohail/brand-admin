/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'res.cloudinary.com', 
      'document-export.canva.com',
      'www.canva.com',
      'canva.com',
      'example.com'
    ],
  },
};

export default nextConfig;
